/* export default function Saludo({ nombre, rol }) {
  //console.log(props)
  //let {nombre} = props
  //let rol = props.rol

  return (
    <h3>
      Hola, soy {nombre} y soy {rol}!
    </h3>
  );
}
 */

const Saludo = ({ nombre, rol }) => {
 return (
    <h3>
      Hola, soy {nombre} y soy {rol}!
    </h3>
  );
}
export default Saludo
